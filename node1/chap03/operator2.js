// === 연산자, !== 연산자
// === 연산자는 == 연산자와 기능이 같고, !== 연산자는 != 연산자와 기능이 같다.
// == 연산자나 != 연산자로 비교할 때는 타입을 엄격하게 비교하지 않지만, === 연산자나 !== 연산자로 비교할 때는 타입을 엄격하게 비교한다.

let a = 3, b = "3";

console.log(a === b);
console.log(a !== b);